package me.tooster.client;

import me.tooster.common.Command;

public enum ClientCommand implements Command {
    ;

    private static final ab
    @Override
    public Command[] cachedValues() {
        return new Command[0];
    }
}
